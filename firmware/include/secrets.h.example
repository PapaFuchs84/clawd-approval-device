#pragma once
// Copy to secrets.h, fill in real values, NEVER commit secrets.h.

#define WIFI_SSID     "<YOUR_WIFI_SSID>"
#define WIFI_PASSWORD "<YOUR_WIFI_PASSWORD>"

// LAN IP of the machine running daemon.py (not the ESP32 itself!)
#define DAEMON_WS_HOST "<LAN_IP_OF_YOUR_DAEMON_HOST>"
#define DAEMON_WS_PORT  8765

// Must exactly match the env vars in daemon/.env
#define AUTH_TOKEN "<SAME_TOKEN_AS_CLAWD_APPROVAL_TOKEN>"
#define HMAC_KEY   "<SAME_KEY_AS_CLAWD_APPROVAL_HMAC_KEY>"
